window.POA_CONFIG = {
  allowedEmail: "sachinbaghele2018@gmail.com",
  /**
   * Create this in Google Cloud Console (free):
   * APIs & Services → Credentials → Create credentials → OAuth client ID → Web application
   * Authorized JavaScript origins must include this site, for example:
   *   http://localhost:5500
   *   http://127.0.0.1:5500
   *   https://your-site.netlify.app
   * Then paste the Client ID below.
   */
  googleClientId: "PASTE_GOOGLE_CLIENT_ID.apps.googleusercontent.com",
};
